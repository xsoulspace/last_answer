/// TASK B (ADR 0015) — the embedded coding-agent host surface: the app's
/// first embedded domain host. The user delegates task sentences, watches
/// streamed progress, and approves writes; everything rides the harness's
/// ACP v1 daemon running in-process (see [HarnessHost]).
library;

export 'agent_doc_surface.dart';
export 'harness_host.dart';
export 'harness_session_controller.dart';
