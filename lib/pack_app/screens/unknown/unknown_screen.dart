part of pack_app;

class UnknownScreen extends StatelessWidget {
  const UnknownScreen({
    final Key? key,
  }) : super(key: key);

  @override
  Widget build(final BuildContext context) => const Scaffold();
}
