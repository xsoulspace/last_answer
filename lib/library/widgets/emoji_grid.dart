part of widgets;

class EmojiGrid extends StatelessWidget {
  const EmojiGrid({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    return Container();
  }
}
