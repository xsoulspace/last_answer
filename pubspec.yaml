name: lastanswer
description: Fast ideas brainstorming tool and quick notes

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 3.22.0+51

environment:
  sdk: ^3.12.0

workspace:
  - packages/*

# to update use flutter pub upgrade --major-versions
dependencies:
  animations: ^2.2.0
  blur: ^4.0.2
  collection: ^1.19.1
  core:
  cryptography: ^2.7.0
  cupertino_icons: ^1.0.9
  dio: ^5.10.0
  equatable: ^2.1.0
  extra_alignments: ^1.0.0+1
  flextras: ^1.0.0
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_hooks: ^0.21.3+1
  flutter_keyboard_visibility: ^6.0.0
  flutter_localizations:
    sdk: flutter
  flutter_native_splash: ^2.4.8
  flutter_portal: ^1.1.4
  flutter_slidable: ^4.0.3
  flutter_svg: ^2.3.0
  form_builder_validators: ^11.3.0
  freezed_annotation: ^3.1.0
  from_json_to_json: ^0.5.0
  gap: ^3.0.1
  go_router: ^17.3.0
  google_fonts: ^8.2.0
  infinite_scroll_pagination: ^5.1.1
  json_annotation: ^4.12.0
  life_hooks: ^0.3.0
  package_info_plus: ^10.2.1
  path_provider: ^2.1.6
  path_to_regexp: ^0.4.0
  provider: ^6.1.5+1
  quiver: ^3.2.2
  recase: ^4.1.0
  rxdart: ^0.28.0
  share_plus: ^13.2.1
  shared_models:
  shared_preferences: ^2.5.5
  ui_kit:
  mcp_toolkit: ^5.0.4
  universal_storage_github_api:
    path: ../dart_flutter_packages/pkgs/universal_storage_github_api
  universal_storage_github_oauth:
    path: ../dart_flutter_packages/pkgs/universal_storage_github_oauth
  universal_storage_filesystem:
    path: ../dart_flutter_packages/pkgs/universal_storage_filesystem
  universal_storage_git_offline:
    path: ../dart_flutter_packages/pkgs/universal_storage_git_offline
  universal_storage_interface:
    path: ../dart_flutter_packages/pkgs/universal_storage_interface
  universal_storage_mesh:
    path: ../dart_flutter_packages/pkgs/universal_storage_mesh
  universal_storage_mesh_transport:
    path: ../dart_flutter_packages/pkgs/universal_storage_mesh_transport
  xsoulspace_inference_acp:
    path: ../dart_flutter_packages/pkgs/xsoulspace_inference_acp
  xsoulspace_inference_core:
    path: ../dart_flutter_packages/pkgs/xsoulspace_inference_core
  # TASK B (ADR 0015): the harness as the app's first embedded domain host.
  # The host module (lib/coding_agent/) owns the daemon lifecycle in-process;
  # the LLM-free scripted seam (handlerFactory) lives in the harness barrel.
  xsoulspace_agentic_harness:
    path: ../dart_flutter_packages/pkgs/xsoulspace_agentic_harness
  xsoulspace_inference_apple_foundation:
    path: ../dart_flutter_packages/pkgs/xsoulspace_inference_apple_foundation
  dart_acp_toolkit:
    path: /Users/antonio/mcp/cline/intentcall/packages/acp_toolkit
  headless_core:
    path: packages/headless_core
  universal_io: ^2.3.1
  url_launcher: ^6.3.2
  uuid: ^4.6.0
  mobile_scanner: ^7.0.1
  qr: ^4.0.0

  intl: any
  xsoulspace_monetization_foundation: any
  xsoulspace_foundation: any
  xsoulspace_installation_store: any
  xsoulspace_monetization_interface: any
  file_selector: any
dev_dependencies:
  flutter_launcher_icons: ^0.14.4
  flutter_test:
    sdk: flutter
  # freezed: ^3.2.3
  lints: ^6.1.0
  # mockito: ^5.5.1
  mocktail: ^1.0.5
  msix: ^3.18.0
  xsoulspace_lints: ^0.1.2

  web: any
dependency_overrides:
  ecsly:
    path: /Users/antonio/xs/ecsly/core_packages/ecsly
  dart_acp_toolkit:
    path: /Users/antonio/mcp/cline/intentcall/packages/acp_toolkit
  xsoulspace_inference_core:
    path: ../dart_flutter_packages/pkgs/xsoulspace_inference_core
  agentic_executables_wire:
    path: ../../agentic_executables/agentic_executables_wire
  ecsly_serialization:
    path: /Users/antonio/xs/ecsly/plugins/ecsly_serialization
  ecsly_async_parallel:
    path: /Users/antonio/xs/ecsly/plugins/ecsly_async_parallel
  async_parallel:
    path: /Users/antonio/xs/ecsly/plugins/async_parallel
  universal_storage_oauth:
    path: ../dart_flutter_packages/pkgs/universal_storage_oauth
  universal_storage_filesystem:
    path: ../dart_flutter_packages/pkgs/universal_storage_filesystem
  universal_storage_git_offline:
    path: ../dart_flutter_packages/pkgs/universal_storage_git_offline
  analyzer: ^14.1.0
  meta: ^1.19.0
  intl: ^0.20.3
  share_plus: ^13.2.1
  xsoulspace_lints:
    path: ../dart_flutter_packages/pkgs/xsoulspace_lints
  dart_style: ^3.1.12
  xsoulspace_ui_foundation:
    path: ../dart_flutter_packages/pkgs/xsoulspace_ui_foundation
  source_gen: ^4.2.3
  xsoulspace_foundation:
    path: ../dart_flutter_packages/pkgs/xsoulspace_foundation
  xsoulspace_locale:
    path: ../dart_flutter_packages/pkgs/xsoulspace_locale
  xsoulspace_state_utils:
    path: ../dart_flutter_packages/pkgs/xsoulspace_state_utils
  # TODO(arenukvern): remove after xsoulspace_monetization_* packages publish
  xsoulspace_installation_store:
    path: ../dart_flutter_packages/pkgs/xsoulspace_installation_store
  xsoulspace_monetization_interface:
    path: ../dart_flutter_packages/pkgs/xsoulspace_monetization_interface
  xsoulspace_monetization_foundation:
    path: ../dart_flutter_packages/pkgs/xsoulspace_monetization_foundation
  xsoulspace_monetization_rustore:
    path: ../dart_flutter_packages/pkgs/xsoulspace_monetization_rustore
  universal_storage_interface:
    path: ../dart_flutter_packages/pkgs/universal_storage_interface
  universal_storage_local_db:
    path: ../dart_flutter_packages/pkgs/universal_storage_local_db
  universal_storage_convergence:
    path: ../dart_flutter_packages/pkgs/universal_storage_convergence
  universal_storage_mesh:
    path: ../dart_flutter_packages/pkgs/universal_storage_mesh
  universal_storage_mesh_transport:
    path: ../dart_flutter_packages/pkgs/universal_storage_mesh_transport
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0

msix_config:
  display_name: "Last Answer: Ideas and Notes"
  publisher_display_name: Anton Malofeev
  identity_name: 31440AntonMalofeev.lastanswer
  publisher: CN=2B407028-2F14-45EB-85E6-15AFF9A915E1
  msix_version: 3.22.0.0
  logo_path: ./packages/core/assets/icons/logo_256.png
  store: true

# fvm dart run flutter_launcher_icons
# generate for macos https://appicon.co
flutter_launcher_icons:
  image_path: "packages/core/assets/icons/logo.png"
  ios: true
  android: true
  remove_alpha_ios: true

# For information see the following page:
# https://pub.dev/packages/flutter_gen
# flutter_gen:
# output: lib/generated/ # Optional (default: lib/gen/)
# # line_length: 80 # Optional (default: 80)
# # Optional
# integrations:
#   flutter_svg: true
#   flare_flutter: true
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter.
flutter:
  generate: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: IBMPlexSans
  #     fonts:
  #       - asset: google_fonts/IBMPlexSans-Thin.ttf
  #         weight: 100
  #       - asset: google_fonts/IBMPlexSans-Light.ttf
  #         weight: 300
  #       - asset: google_fonts/IBMPlexSans-Regular.ttf
  #         weight: 400
  #       - asset: google_fonts/IBMPlexSans-Medium.ttf
  #         weight: 500
  #       - asset: google_fonts/IBMPlexSans-SemiBold.ttf
  #         weight: 700
  #       - asset: google_fonts/IBMPlexSans-Bold.ttf
  #         weight: 900
  #   - family: NotoColorEmoji
  #     fonts:
  #       - asset: google_fonts/NotoColorEmoji.ttf
  #   - family: NotoSans
  #     fonts:
  #       - asset: google_fonts/NotoSans-Regular.ttf
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

# https://pub.dev/packages/flutter_native_splash
flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # flutter pub run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # flutter pub run flutter_native_splash:remove
  # color or background_image is the only required parameter.  Use color to set the background
  # of your splash screen to a solid color.  Use background_image to set the background of your
  # splash screen to a png image.  This is useful for gradients. The image will be stretch to the
  # size of the app. Only one parameter can be used, color and background_image cannot both be set.
  color: "#F8F9F7"
  #background_image: "assets/background.png"
  # Optional parameters are listed below.  To enable a parameter, uncomment the line by removing
  # the leading # character.
  # The image parameter allows you to specify an image used in the splash screen.  It must be a
  # png file and should be sized for 4x pixel density.
  #image: assets/splash.png
  # The color_dark, background_image_dark, and image_dark are parameters that set the background
  # and image when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above. If the image_dark parameter is specified, color_dark or
  # background_image_dark must be specified.  color_dark and background_image_dark cannot both be
  # set.
  color_dark: "#212121"
  #background_image_dark: "assets/dark-background.png"
  #image_dark: assets/splash-invert.png
  # The android, ios and web parameters can be used to disable generating a splash screen on a given
  # platform.
  #android: false
  #ios: false
  #web: false
  # The position of the splash image can be set with android_gravity, ios_content_mode, and
  # web_image_mode parameters.  All default to center.
  #
  # android_gravity can be one of the following Android Gravity (see
  # https://developer.android.com/reference/android/view/Gravity): bottom, center,
  # center_horizontal, center_vertical, clip_horizontal, clip_vertical, end, fill, fill_horizontal,
  # fill_vertical, left, right, start, or top.
  #android_gravity: center
  #
  # ios_content_mode can be one of the following iOS UIView.ContentMode (see
  # https://developer.apple.com/documentation/uikit/uiview/contentmode): scaleToFill,
  # scaleAspectFit, scaleAspectFill, center, top, bottom, left, right, topLeft, topRight,
  # bottomLeft, or bottomRight.
  #ios_content_mode: center
  #
  # web_image_mode can be one of the following modes: center, contain, stretch, and cover.
  #web_image_mode: center
  # To hide the notification bar, use the fullscreen parameter.  Has no affect in web since web
  # has no notification bar.  Defaults to false.
  # NOTE: Unlike Android, iOS will not automatically show the notification bar when the app loads.
  #       To show the notification bar, add the following code to your Flutter app:
  #       WidgetsFlutterBinding.ensureInitialized();
  #       SystemChrome.setEnabledSystemUIOverlays([SystemUiOverlay.bottom, SystemUiOverlay.top]);
  # fullscreen: true
  # If you have changed the name(s) of your info.plist file(s), you can specify the filename(s)
  # with the info_plist_files parameter.  Remove only the # characters in the three lines below,
  # do not remove any spaces:
  #info_plist_files:
  #  - 'ios/Runner/Info-Debug.plist'
  #  - 'ios/Runner/Info-Release.plist'
