name: landing
description: A pure client-side app without ssr.
version: 0.0.1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  jaspr: ^0.13.1
  jaspr_router: ^0.4.2

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.10
  collection: ^1.18.0
  jaspr_builder: ^0.13.1
  lints: ^4.0.0

jaspr:
  mode: client
