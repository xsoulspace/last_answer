name: core_server_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  serverpod: 1.1.1
  serverpod_auth_server: 1.1.1
  shared_models:
    path: ../../packages/shared_models

dev_dependencies:
  flutter_lints: ^3.0.0
  xsoulspace_lints: ^0.0.10

dependency_overrides:
  connectivity_plus: ^5.0.2
  flutter_lints: ^3.0.0
  google_fonts: ^6.1.0
  lints: ^3.0.0
  http: ^1.1.2
  uuid: ^4.2.1
