part of pack_auth;

class SignInScreen extends HookWidget {
  const SignInScreen({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    final state = useSignInScreenState();

    return Container();
  }
}
