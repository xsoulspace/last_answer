class: Purchase
table: purchases
fields:
  # TODO: replace to Enum
  # GooglePlay, AppStore, RuStore, Web
  source: int
  # TODO: replace to Enum
  # pending, active, expired
  status: int
  purchase_date: DateTime
  expiry_date: DateTime
  userId: int, parent=users
  orderId: String
  productId: String
